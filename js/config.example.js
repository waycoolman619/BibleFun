/**
 * Optional: AI announcer (OpenAI TTS)
 * 1. Copy this file to config.js (in the same js/ folder).
 * 2. Get an API key from https://platform.openai.com/api-keys
 * 3. Set it below. Keep config.js private (e.g. add to .gitignore).
 * 4. In index.html, add before other scripts: <script src="js/config.js"></script>
 *
 * If you don't set this, the app uses the browser's built-in voice for announcements.
 */
window.BIBLE_FUN_OPENAI_API_KEY = '';
